# Constant across all captures (dropped): pwfs (plywood-top body), jf/ir tokens.
# Names are a grab-bag of distinct IR render variants (tap length, sample rate,
# the "5050" and "cuki" renders) that do not factor into real control axes,
# so they map to a single clean 'preset' axis.
manifest_version: 1
id: ir_plywood_top_bc_rich_grand_auditorium
display_name: Plywood Top BC Rich Grand Auditorium
brand: bc-rich
type: body
backend: ir
parameters:
- name: preset
  display_name: Preset
  values:
  - jf45
  - jf45_1024
  - jf45_5050
  - cuki_2048
captures:
- values:
    preset: cuki_2048
  file: ir/cukijfirpwfs2048at48.wav
  output_gain_db: -15.0448046
- values:
    preset: jf45_5050
  file: ir/jf45ir5050pwfs.wav
  output_gain_db: -3.5065877
- values:
    preset: jf45_1024
  file: ir/jf45ir1024pwfs.wav
  output_gain_db: -6.7307444
- values:
    preset: jf45
  file: ir/jf45irpwfs.wav
  output_gain_db: -8.1549644
