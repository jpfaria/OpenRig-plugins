# Community capture: Rectifier Setup (da_impact). Undocumented controls, so each real capture name
# is a preset value (grab-bag selector).
manifest_version: 1
id: ir_mesa_rectifier_da_impact
display_name: Rectifier Setup (da_impact)
brand: mesa
type: cab
backend: ir
parameters:
- name: preset
  display_name: Preset
  values:
  - at4033
  - sm57
captures:
- values:
    preset: at4033
  file: ir/at4033.wav
  output_gain_db: -20.0350609
- values:
    preset: sm57
  file: ir/sm57.wav
  output_gain_db: -20.5043144
