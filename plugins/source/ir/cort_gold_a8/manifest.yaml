# Constant across all captures (dropped): brand/model slug "cort_gold_a8" and sample rate "48000".
# Remaining suffixes are a grab-bag of named IR treatments (none, bld, jf_flavor, match)
# that do not decompose into real control axes → single 'preset' axis.
manifest_version: 1
id: ir_cort_gold_a8
display_name: Gold A8
brand: cort
type: body
backend: ir
parameters:
- name: preset
  display_name: Preset
  values:
  - flat
  - bld
  - jf
  - match
captures:
- values:
    preset: flat
  file: ir/cort_gold_a8_48000.wav
  output_gain_db: 3.1915789
- values:
    preset: bld
  file: ir/cort_gold_a8_48000_bld.wav
  output_gain_db: 8.3812265
- values:
    preset: jf
  file: ir/cort_gold_a8_48000_jf_flavor.wav
  output_gain_db: 9.0578680
- values:
    preset: match
  file: ir/cort_gold_a8_48000_match.wav
  output_gain_db: 5.9185181
