manifest_version: 1
id: vst3_zl_equalizer
display_name: EQ ZLEqualizer
description: ZLEqualizer — dynamic parametric EQ with per-band side-chain and analyser by ZL-Audio.
brand: zl-audio
license: AGPL-3.0-or-later
homepage: https://github.com/ZL-Audio/ZLEqualizer
type: vst3
backend: vst3
bundle: bundles/ZLEqualizer.vst3
# No manifest-authored knobs: OpenRig hosts VST3 via its NATIVE editor and
# discovers parameters at runtime from the plugin's IEditController
# (crates/vst3/src/host.rs). The manifest parameters[] block is not consumed by
# the engine for VST3, so it stays empty.
parameters: []
