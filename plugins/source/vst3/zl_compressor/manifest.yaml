manifest_version: 1
id: vst3_zl_compressor
display_name: Compressor ZLCompressor
description: ZLCompressor — feature-rich dynamics compressor by ZL-Audio.
brand: zl-audio
license: AGPL-3.0-or-later
homepage: https://github.com/ZL-Audio/ZLCompressor
type: vst3
backend: vst3
bundle: bundles/ZLCompressor.vst3
# No manifest-authored knobs: OpenRig hosts VST3 via its NATIVE editor and
# discovers parameters at runtime from the plugin's IEditController
# (crates/vst3/src/host.rs). The manifest parameters[] block is not consumed by
# the engine for VST3, so it stays empty.
parameters: []
