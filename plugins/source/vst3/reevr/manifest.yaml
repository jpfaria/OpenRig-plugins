manifest_version: 1
id: vst3_reevr
display_name: Reverb reevr
description: REEV-R — convolution reverb with modulation and post-processing by tiagolr.
brand: tiagolr
license: GPL-3.0-or-later
homepage: https://github.com/tiagolr/reevr
type: vst3
backend: vst3
bundle: bundles/REEV-R.vst3
# ~2187 runtime parameters (a per-point automation grid /
# modular node graph), far beyond a hand-groupable knob wall. Parameters are
# discovered at runtime from the plugin's IEditController and the app groups
# them dynamically by VST3 unit + name-prefix (issue #117); no manual groups.
parameters: []
