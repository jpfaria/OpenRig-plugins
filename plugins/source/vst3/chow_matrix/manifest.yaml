manifest_version: 1
id: vst3_chow_matrix
display_name: Delay ChowMatrix
description: ChowMatrix — growable multitap delay with per-node modulation and feedback by ChowDSP.
brand: chowdsp
license: BSD-3-Clause
homepage: https://github.com/Chowdhury-DSP/ChowMatrix
type: vst3
backend: vst3
bundle: bundles/ChowMatrix.vst3
# ~667 runtime parameters (a per-point automation grid /
# modular node graph), far beyond a hand-groupable knob wall. Parameters are
# discovered at runtime from the plugin's IEditController and the app groups
# them dynamically by VST3 unit + name-prefix (issue #117); no manual groups.
parameters: []
